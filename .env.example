# Optional. When set to a postgres:// or postgresql:// URL, TKA student
# progress (lessons, streaks, XP, tryouts) is saved in Postgres.
# Locally, progress is written to data/tka-store.json even without this.
DATABASE_URL=
